machine:
  services:
    - docker
dependencies:
  override:
    - docker info
    - docker build -t dynamodblocal circleci/.
test:
  pre:
    - bundle exec rubocop
  override:
    - docker run -d -p 8000:8000 -t dynamodblocal
    - bundle exec rspec
